import 'package:flutter/material.dart';
class FoodDrawer extends StatelessWidget {
  const FoodDrawer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Drawer();
  }
}
